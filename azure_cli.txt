# To list all the VMs
az vm list -d -o json

# To start a VM
az vm start \
    --resource-group RG \
    --name VM

# To stop a VM
az vm stop \
    --resource-group RG \
    --name VM

# To restart a VM
az vm restart

